__pycache__/
*.pyc
.venv/
venv/
.last_commit_hash
.pending_validation
.validation_error_log
logs/
*.log
.env
.portfolio_config.json
.demandes_modifs.json

# Bases SQLite — ne JAMAIS versionner (jetons de session, données utilisateurs)
*.db
*.sqlite
*.sqlite3
*.db-journal
*.db-wal
*.db-shm

.DS_Store
Thumbs.db

# Config tunnel locale (chemins absolus user-spécifiques, généré par le bootstrap)
mnwork.yml

# Claude Code — permissions locales et log auto-push (déjà couvert par *.log mais explicite)
.claude/settings.local.json
.claude/auto-push.log
# Worktrees git internes — ne JAMAIS versionner (audit M11)
.claude/worktrees/
